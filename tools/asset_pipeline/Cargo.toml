[package]
name = "gameengine-asset-tool"
version = "0.1.0"
edition = "2021"
description = "Offline GameEngine asset format and import pipeline"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
