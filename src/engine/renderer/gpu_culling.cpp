#include "engine/renderer/gpu_culling.hpp"

namespace gameengine::renderer::gpu_culling {

// The GPU-facing layouts are intentionally defined in the header so that the
// shader-side byte contracts are checked by the renderer and unit tests.

} // namespace gameengine::renderer::gpu_culling
